# Runtime dependencies for the Django application. Keep direct dependencies
# pinned so local development and deployment use the same versions.
Django==6.0.7
django-crispy-forms==2.6
crispy-tailwind==1.0.3
django-fsm-2==4.2.4

